# Notebook Templates

This folder contains the standard notebook template for agent system experiments.

## How to use

1. Copy `notebook-template.md` as a reference when creating new notebooks
2. Follow the naming convention: `YYYY-MM-DD_topic.ipynb`
3. Keep notebooks focused on exploration — not production logic
4. Move stable agent logic into `agents/`, `tools/`, or `orchestration/`
