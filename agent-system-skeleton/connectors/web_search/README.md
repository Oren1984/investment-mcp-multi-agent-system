# Purpose

Optional web search connector placeholder for agent projects.

Some agents may use web search as a tool,
but not every agent requires it.

# Important

This connector should remain:
- optional
- disabled by default
- clearly separated from core agent logic