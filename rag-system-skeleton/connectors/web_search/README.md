# Purpose

Optional web search connector placeholder for RAG projects.

This exists for cases where a project may combine:
- internal retrieval
- external web retrieval

# Important

Web search is optional.
It should stay disabled by default.
This template does not provide a real web search implementation.