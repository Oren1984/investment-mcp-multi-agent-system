# Infrastructure

## Purpose
This folder contains infrastructure-related assets and placeholders for environment setup and deployment support.

## Main Areas
- `docker/` - Containerization assets
- `terraform/` - Infrastructure as code assets
- `scripts/` - Helper scripts for setup and operations

## Notes
Infrastructure should stay separated from application logic so environments can evolve independently.