# Purpose

This folder holds the minimal API surface for a RAG project.

The template only provides route structure ideas.
It does not provide a production-ready backend.

# Typical endpoints

- health check
- query endpoint
- retrieval debug endpoint
- citation preview endpoint

# Important

Keep API thin.
Business logic should stay outside route files.