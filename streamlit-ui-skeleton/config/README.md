# Purpose

This folder documents configuration conventions for the Streamlit UI.

Examples:
- environment variable rules
- backend base URL guidance
- feature flags
- local vs production settings
- theme guidance

Do not place secrets directly in the project files.
Use `.env.example` as the reference.