# Purpose

This folder contains service-layer code used by the UI.

Examples:
- FastAPI backend requests
- health checks
- chat API calls
- RAG query requests
- auth/session calls
- structured request/response helpers

The UI should call service functions instead of embedding raw request logic everywhere.