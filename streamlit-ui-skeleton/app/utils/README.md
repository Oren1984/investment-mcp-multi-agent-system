# Purpose

This folder contains helper functions shared across the Streamlit UI.

Examples:
- formatting helpers
- validation helpers
- session utilities
- state helpers
- file parsing helpers
- display helpers

Keep generic utility logic here instead of inside page files.