# Purpose

This folder contains reusable Streamlit UI components.

Examples:
- sidebar sections
- metric cards
- result renderers
- chat message blocks
- file upload blocks
- filters
- status banners

Use this folder to avoid repeating UI code across pages.