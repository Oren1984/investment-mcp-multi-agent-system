apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: investment-system
spec:
  selector:
    app: investment-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
